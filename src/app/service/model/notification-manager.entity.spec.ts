import { NotificationManagerEntity } from './notification-manager.entity';

describe('NotificationManagerEntity', () => {
  it('should create an instance', () => {
    expect(new NotificationManagerEntity()).toBeTruthy();
  });
});
