import { InterventionEntity } from './intervention.entity';

describe('InterventionEntity', () => {
  it('should create an instance', () => {
    expect(new InterventionEntity()).toBeTruthy();
  });
});
