import { ProductInventoryEntity } from './product-stock.entity';

describe('ProductInventoryEntity', () => {
  it('should create an instance', () => {
    expect(new ProductInventoryEntity()).toBeTruthy();
  });
});
