import { ProductTypeEntity } from './product-type.entity';

describe('ProductTypeEntity', () => {
  it('should create an instance', () => {
    expect(new ProductTypeEntity()).toBeTruthy();
  });
});
